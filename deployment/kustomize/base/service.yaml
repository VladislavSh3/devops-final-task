apiVersion: v1
kind: Service
metadata:
  name: sheverev-devops-final-task-service
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: sheverev-devops-final-task
  type: NodePort
