apiVersion: v1
kind: Service
metadata:
  name: sheverev-devops-final-task-service
spec:
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app: sheverev-devops-final-task
  type: ClusterIP
